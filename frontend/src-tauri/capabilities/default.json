{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Default-deny desktop window with only core webview permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

